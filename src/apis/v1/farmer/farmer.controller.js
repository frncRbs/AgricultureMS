const { sendResponse, setConnection } = require('server');

class FarmerController {
    async requestServices(req, res, next) {}

    async requestCrops(req, res, next) {}

    async viewRequestHistory(req, res, next) {}
}

module.exports = FarmerController;