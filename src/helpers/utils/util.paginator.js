/**
 * It returns the number of pages and the number of rows.
 */
// const paginator = ({ numberPerPage, numRows, page }) => {
//     let _quaryPagination;
//     let _numberPerPage = parseInt(numberPerPage, 10) || 1;
//     let _page = parseInt(page, 10) || 1;
//     let _numPages;
//     let _skip = page * _numberPerPage;

//     _numPages = Math.ceil(numRows / _numberPerPage);
// };

// module.exports = { paginator };
